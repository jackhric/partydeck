[package]
name = "partydeck-comp-proto"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
ts = ["dep:ts-rs"]

[dependencies]
serde.workspace = true
serde_json.workspace = true
ts-rs = { version = "12.0", features = ["serde-compat"], optional = true }

[dev-dependencies]
tempfile = "3"
